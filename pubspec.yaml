name: clima
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  geolocator: ^5.3.1
  http: ^0.12.1
  font_awesome_flutter: ^8.8.0
  flutter_spinkit: "^4.1.2"

dependency_overrides:
  font_awesome_flutter:
    path: "/Users/Anthony/Code/Flutter/CustomPub/font_awesome_flutter"
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
  - images/

  fonts:
  - family: Spartan MB
    fonts:
    - asset: fonts/SpartanMB-Black.otf
      weight: 900